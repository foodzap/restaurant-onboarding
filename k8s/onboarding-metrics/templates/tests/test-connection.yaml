apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "onboarding-metrics.fullname" . }}-test-connection"
  labels:
    {{- include "onboarding-metrics.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "onboarding-metrics.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
